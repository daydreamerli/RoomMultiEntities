<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookmvvm">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.bookmvvm">
        <activity android:name=".UpdateBookActivity"></activity>
        <activity android:name=".UpdateBook" />
        <activity android:name=".AddFromCActivity" />
        <activity android:name=".AddCd" />
        <activity android:name=".CdDetailsActivity" />
        <activity android:name=".CdMainActivity" />
        <activity android:name=".CatagoryActivity" />
        <activity android:name=".BookMainActivity" />
        <activity android:name=".BookDetailsActivity" />
        <activity android:name=".AddBook" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>